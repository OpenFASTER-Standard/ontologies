# KaFE — Business Rules

Real conditional-mandatory logic (e.g. the real §50j EStG "7xxx" range,
where the raw XSD says `minOccurs=0` but real handbook-only rules make the
field conditionally required) currently exists nowhere machine-readable.
Not thin like `mikadiv-vib/rules/` -- KaFE's own handbook is the primary
source for this layer. **Status: empty, not yet started** (sub-project 5).
