# KaFE — Raw Sources

The v1.5.0 KHB (German+English) and all 6 real XSDs
(`kafe`/`kafe-rm`/`kafe-va`/`kafe-standardtypes`/`kafe-statustypes`/
`kafe-isotypes`). **Status: empty, not yet copied in** (sub-project 5).
The 213-code status/error catalog's real handbook source is currently
preserved nowhere (only a hand-transcription survives from prior,
deprecated work) -- locating and committing the actual source PDF here is
part of sub-project 5, not this scaffolding.
