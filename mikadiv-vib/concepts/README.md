# MiKaDiv-VIB — Concepts (`MVO:`)

Semantic layer only -- no serialization facets. **Status: skeleton only,
no concepts curated yet.** Build/verify with
`../../scripts/build-module.sh mikadiv-vib/concepts mikadiv-vib-concepts mikadiv-vib-concepts`
from the repo root.
